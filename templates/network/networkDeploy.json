{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "baseSettings": {
            "type": "object"
        },
        "vnetObject": {
            "type": "secureObject"
        },
        "sasToken": {
            "type": "securestring"
        }
    },
    "variables": {
        "nameDefaults": {
            "vnetBaseName": "[concat('vn-', parameters('basesettings').env,'-')]"
        }
    },
    "resources": [
        {
            "apiVersion": "2017-05-10",
            "name": "[concat('lnk-', variables('nameDefaults').vnetBaseName, parameters('vnetObject').settings[copyIndex()].name, '-', copyIndex())]",
            "type": "Microsoft.Resources/deployments",
            "properties": {
                "mode": "Incremental",
                "templateLink": {
                    "uri": "[concat(parameters('baseSettings').baseUrl, 'vnet/vnetLoader.json',  parameters('sasToken'))]",
                    "contentVersion": "1.0.0.0"
                },
                "parameters": {
                    "singleVnetObject": {
                        "value": "[parameters('VnetObject').settings[copyIndex()]]"
                    },
                    "vnetName": {
                        "value": "[concat(variables('nameDefaults').vnetBaseName, parameters('VnetObject').settings[copyIndex()].name)]"
                    },
                    "sasToken": {
                        "value": "[parameters('sasToken')]"
                    }
                }
            },
            "copy": {
                "name": "lnkvnetCopy",
                "count": "[length(parameters('vnetObject').settings)]"
            }
        },
        {
            "apiVersion": "2017-05-10",
            "name": "[concat('lnk-', variables('nameDefaults').vnetBaseName, parameters('vnetObject').settings[copyIndex()].name, '-peers-', copyIndex())]",
            "type": "Microsoft.Resources/deployments",
            "properties": {
                "mode": "Incremental",
                "templateLink": {
                    "uri": "[concat(parameters('baseSettings').baseUrl, 'vnet/peerLoader.json',  parameters('sasToken'))]",
                    "contentVersion": "1.0.0.0"
                },
                "parameters": {
                    "singleVnetObject": {
                        "value": "[parameters('VnetObject').settings[copyIndex()]]"
                    },
                    "vnetName": {
                        "value": "[concat(variables('nameDefaults').vnetBaseName, parameters('VnetObject').settings[copyIndex()].name)]"
                    },
                    "nameDefaults": {
                        "value": "[variables('nameDefaults')]"
                    }
                }
            },
            "dependsOn": [
                "lnkvnetCopy"
            ],
            "copy": {
                "name": "lnkpeerCopy",
                "count": "[length(parameters('vnetObject').settings)]",
                "mode": "Parallel"
            }
        },
        {
            "apiVersion": "2017-05-10",
            "name": "[concat('lnk-', variables('nameDefaults').vnetBaseName, parameters('vnetObject').settings[copyIndex()].name, '-connection-', copyIndex())]",
            "type": "Microsoft.Resources/deployments",
            "condition": "[equals(parameters('vnetObject').settings[copyindex()].isVPNGW, bool('true'))]",
            "properties": {
                "mode": "Incremental",
                "templateLink": {
                    "uri": "[concat(parameters('baseSettings').baseUrl, 'vnet/deployVPN.json',  parameters('sasToken'))]"
                },
                "parameters": {
                    "singleVnetObject": {
                        "value": "[parameters('vnetObject').settings[copyindex()]]"
                    },
                    "vnetName": {
                        "value": "[concat(variables('nameDefaults').vnetBaseName, parameters('VnetObject').settings[copyIndex()].name)]"
                    }
                }
            },
            "copy": {
                "name": "ConnectionCopy",
                "count": "[length(parameters('vnetObject').settings)]",
                "mode": "Parallel"
            },
            "dependsOn": [
                "lnkvnetCopy"
            ]
        }
    ]
}