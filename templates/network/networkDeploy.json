{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "baseSettings": {
            "type": "object"
        },
        "networkObject": {
            "type": "secureObject"
        },
        "sasToken": {
            "type": "securestring"
        }
    },
    "variables": {
        "nameDefaults": {
            "vnetBaseName": "[concat('vn-', parameters('basesettings').env,'-')]"
        }
    },
    "resources": [
        {
            "apiVersion": "2017-05-10",
            "name": "[concat('lnk-', variables('nameDefaults').vnetBaseName, parameters('networkObject').vNets[copyIndex()].name, '-', copyIndex())]",
            "type": "Microsoft.Resources/deployments",
            "properties": {
                "mode": "Incremental",
                "templateLink": {
                    "uri": "[concat(parameters('baseSettings').baseUrl, 'vnet/vnetLoader.json',  parameters('sasToken'))]",
                    "contentVersion": "1.0.0.0"
                },
                "parameters": {
                    "singleVnetObject": {
                        "value": "[parameters('networkObject').vNets[copyIndex()]]"
                    },
                    "vnetName": {
                        "value": "[concat(variables('nameDefaults').vnetBaseName, parameters('networkObject').vNets[copyIndex()].name)]"
                    },
                    "sasToken": {
                        "value": "[parameters('sasToken')]"
                    }
                }
            },
            "copy": {
                "name": "lnkvnetCopy",
                "count": "[length(parameters('networkObject').vNets)]"
            }
        },
        {
            "apiVersion": "2017-05-10",
            "name": "[concat('lnk-', variables('nameDefaults').vnetBaseName, parameters('networkObject').vNets[copyIndex()].name, '-peers-', copyIndex())]",
            "type": "Microsoft.Resources/deployments",
            "properties": {
                "mode": "Incremental",
                "templateLink": {
                    "uri": "[concat(parameters('baseSettings').baseUrl, 'vnet/peerLoader.json',  parameters('sasToken'))]",
                    "contentVersion": "1.0.0.0"
                },
                "parameters": {
                    "singleVnetObject": {
                        "value": "[parameters('networkObject').vNets[copyIndex()]]"
                    },
                    "vnetName": {
                        "value": "[concat(variables('nameDefaults').vnetBaseName, parameters('networkObject').vNets[copyIndex()].name)]"
                    },
                    "nameDefaults": {
                        "value": "[variables('nameDefaults')]"
                    }
                }
            },
            "dependsOn": [
                "lnkvnetCopy"
            ],
            "copy": {
                "name": "lnkpeerCopy",
                "count": "[length(parameters('networkObject').vNets)]",
                "mode": "Parallel"
            }
        },
        {
            "apiVersion": "2017-05-10",
            "name": "[concat('lnk-', variables('nameDefaults').vnetBaseName, parameters('networkObject').vNets[copyIndex()].name, '-connection-', copyIndex())]",
            "type": "Microsoft.Resources/deployments",
            "condition": "[greater(length(parameters('networkObject').vNets[copyindex()].vpnConnections), 0)]",
            "properties": {
                "mode": "Incremental",
                "templateLink": {
                    "uri": "[concat(parameters('baseSettings').baseUrl, 'vnet/deployVPN.json',  parameters('sasToken'))]"
                },
                "parameters": {
                    "singleVnetObject": {
                        "value": "[parameters('networkObject').vNets[copyindex()]]"
                    },
                    "vnetName": {
                        "value": "[concat(variables('nameDefaults').vnetBaseName, parameters('networkObject').vNets[copyIndex()].name)]"
                    }
                }
            },
            "copy": {
                "name": "ConnectionCopy",
                "count": "[length(parameters('networkObject').vNets)]",
                "mode": "Parallel"
            },
            "dependsOn": [
                "lnkvnetCopy"
            ]
        }
    ]
}